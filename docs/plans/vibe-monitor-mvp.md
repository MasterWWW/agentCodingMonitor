# Vibe Monitor MVP

Implementation plan archive. See repository root and [architecture.md](../architecture.md) for the shipped design.

Milestones: core + hook binary → install-hooks → Tauri UI + wizard → Claude/Codex + lite mode → Windows parity → open source docs + CI.
