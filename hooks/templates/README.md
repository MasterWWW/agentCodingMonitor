# Hook templates

Runtime installation is performed by the desktop app (`install_hooks_cmd`) or `POST /api/install-hooks`.

These fragments document the expected shape; placeholders `{{VIBE_HOOK}}` are replaced with the installed binary path.
